# 斗地主回放功能 - 完整方案 v2

## 目标
回放像看录像：叫地主 → 拿底牌 → 出牌 → 结束
三个人的牌全部摊开，等比例缩小

## 只改 index.html，后端不动

---

## 第1步：记录初始牌局

**改哪里：** dealRound 函数，发完牌、确定地主之后

**存什么：**
```
gameMoves[0] = {
  type: "deal",
  myHand: ["3","4","5",...,"小王"],     // 你的牌
  leftHand: ["3","4","5",...,"2"],       // 电脑A的牌
  rightHand: ["3","4","5",...,"2"],      // 电脑B的牌
  landlord: 0,                           // 谁是地主 (0=你,1=A,2=B)
  landlordCards: ["3","5","7"]           // 底牌
}
```

---

## 第2步：记录叫地主过程

**改哪里：** doBidTurn 函数，每次叫/不叫/抢/不抢时

**存什么：**
```
gameMoves.push({
  type: "bid",
  player: 0/1/2,          // 谁
  action: "叫地主"/"不叫"/"抢地主"/"不抢",
  multiplier: 2/4/8       // 当前倍数
})
```

**回放时效果：**
```
第1秒：显示"电脑A：叫地主（2倍）"
第2.5秒：显示"你：不叫"
第4秒：显示"电脑B：抢地主（4倍）"
第5.5秒：显示"电脑A：不抢"
→ 电脑B成为地主，拿到底牌
```

---

## 第3步：记录出牌（已有的，不用改）

**现在已经在 doPlayCards 里记录了：**
```
gameMoves.push({
  type: "play",
  player: "你"/"电脑A"/"电脑B",
  pattern: "单张"/"对子"/"顺子",
  cards: "3"/"A,A"/"3,4,5,6,7"
})
```

**以及 doPass 里的：**
```
gameMoves.push({
  type: "pass",
  player: "你"/"电脑A"/"电脑B"
})
```

---

## 第4步：回放模式初始化

**改哪里：** initReplayMode 函数

**做什么：**
1. 隐藏开始弹窗、出牌按钮、叫地主按钮
2. 显示回放控制条（底部）
3. 根据 gameMoves[0] 渲染初始牌局

**渲染三个人的牌（等比例缩小）：**

你的牌（底部）：
  正常大小，跟真实游戏一模一样

电脑A的牌（左上）：
  同样的排列方式，宽度和高度都缩小到65%
  牌面朝上，能看到每张牌
  用一个区域框起来，标记"电脑A"

电脑B的牌（右上）：
  同样的排列方式，宽度和高度都缩小到65%
  牌面朝上，能看到每张牌
  用一个区域框起来，标记"电脑B"

底牌（顶部居中）：
  正常大小，显示3张底牌

---

## 第5步：自动回放叫地主

**改哪里：** startReplayPlayback 函数

**流程：**
```
遍历 gameMoves：

遇到 type=="bid"：
  → 屏幕中央显示提示："电脑A：叫地主（2倍）"
  → 更新倍数显示
  → 等待 1.5秒/速度
  → 继续下一步

遇到 type=="deal"：
  → 跳过（初始化时已处理）
```

---

## 第6步：自动回放出牌

**遇到 type=="play"：**
```
1. 从出牌者的手牌中移走这些牌
2. 在出牌区显示这些牌
3. 显示提示："你出了 对A" / "电脑A出了 顺子 3-4-5-6-7"
4. 更新三个人的手牌显示（移走的牌消失）
5. 如果是炸弹/王炸，播放特效
6. 等待 1.5秒/速度
```

**遇到 type=="pass"：**
```
1. 在出牌者位置显示"过"
2. 等待 1.5秒/速度
```

---

## 第7步：控制条

**位置：** 屏幕底部固定

**内容：**
```
[1x] [2x] [4x] [8x]    第5步/共38步    [退出回放]
```

**倍速：**
- 1x = 1.5秒/步
- 2x = 0.75秒/步
- 4x = 0.375秒/步
- 8x = 0.1875秒/步

---

## 第8步：回放结束

**触发：** 所有 gameMoves 执行完毕

**效果：**
- 屏幕中央显示一行字："游戏结束 - 你赢了" 或 "游戏结束 - 电脑B赢了"
- 2秒后文字消失
- 不弹大弹窗
- 底部"退出回放"按钮仍然可用

---

## 第9步：禁用回放模式下的操作

**隐藏：**
- 出牌按钮（不出/出牌/提示）
- 叫地主按钮（叫地主/不叫/抢地主/不抢）
- 手牌点击（不能选牌）

**保持：**
- 倍速切换
- 退出回放

---

## gameMoves 最终格式

```javascript
[
  {type:"deal", myHand:[...], leftHand:[...], rightHand:[...], landlord:1, landlordCards:[...]},

  {type:"bid", player:1, action:"叫地主", multiplier:2},
  {type:"bid", player:0, action:"不叫", multiplier:2},
  {type:"bid", player:2, action:"抢地主", multiplier:4},
  {type:"bid", player:1, action:"不抢", multiplier:4},

  {type:"play", player:2, pattern:"对子", cards:"K,K"},
  {type:"play", player:0, pattern:"对子", cards:"A,A"},
  {type:"pass", player:1},
  {type:"play", player:0, pattern:"顺子", cards:"3,4,5,6,7"},
  ...
]
```

---

## 改动范围

| 文件 | 改什么 |
|------|--------|
| index.html - dealRound | 记录初始牌局到 gameMoves[0] |
| index.html - doBidTurn | 记录叫地主过程 |
| index.html - initReplayMode | 重写：渲染初始牌局 + 控制条 |
| index.html - startReplayPlayback | 重写：按顺序执行每一步 |
| index.html - 新增 renderReplayHands | 渲染三个人的牌（等比例缩小） |
| index.html - 新增 executeReplayStep | 执行单步回放 |
| index.html - 禁用交互 | 回放模式下隐藏操作按钮 |
| backend/app.py | 不改 |
| lobby.html | 不改 |

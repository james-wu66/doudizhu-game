# 斗地主手机端两个小问题修复方案

## 问题1：手机回放牌太大

**现状：** 回放模式的牌尺寸（48×67px 电脑手牌、45×63px 出牌区）是按电脑屏幕设计的，手机屏幕小但牌还是电脑大小，导致牌太大放不下。

**方案：** 在 index.html 的 CSS 里，加一个手机端媒体查询，缩小回放模式的牌尺寸。

**改动：**
- 在 `</style>` 前加一段 `@media (max-width:767px)` 针对 `body.replay-mode` 的规则
- 电脑手牌：48×67 → 约 28×40（缩小约 40%）
- 出牌区牌：45×63 → 约 26×37
- 容器宽度等比缩小
- 只影响回放模式，不影响正常游戏

**改动文件：** `frontend/index.html`（加 CSS）

---

## 问题2：手机点设置时底牌轮廓还在

**现状：** 设置弹窗 z-index 是 1500，但手机端媒体查询把 `#landlord-area` 设成了 `z-index:2000!important`（行433），比设置弹窗还高，导致底牌穿透设置弹窗。

**方案：** 在回放模式或设置弹窗打开时，给 landlord-area 加一个更高的 z-index，或者在设置弹窗的 CSS 里加 `z-index:9999!important` 确保它永远在最上面。

**改动：**
- `#settings-modal` 的 z-index 从 1500 改成 9999（一行改动）
- 确保不影响其他功能

**改动文件：** `frontend/index.html`（改一行 CSS）

---

## 修改纪律
- 只改这两处 CSS，不动其他代码
- 改完自测：正常游戏 + 回放 + 设置弹窗
- 推到 GitHub 自动部署

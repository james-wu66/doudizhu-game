# 手机端+回放 缩放调整方案

## 目标
1. 手机回放：手牌+出牌区+底牌 全部等比缩小
2. 电脑回放：出牌区牌太小，调大
3. 手机正常游戏：底牌缩小10%并上移
4. 所有缩小都是整体缩小（牌+花纹+数字一起缩）

## 约束
- 电脑正常游戏不动
- 回放的桌面端不动（除了出牌区调大）
- 只改CSS+JS缩放系数，不动游戏逻辑

---

## 改动1：手机回放手牌+出牌区再缩小

**现状：** replayScale=0.64，用户说还是大
**方案：** 改成 0.52（再缩20%）

| 项目 | 电脑(0.8) | 手机现在(0.64) | 手机改后(0.52) |
|------|----------|---------------|---------------|
| 牌宽 | ≈45px | ≈36px | ≈29px |
| 牌高 | ≈63px | ≈50px | ≈41px |

**改法：** JS `var replayScale=(window.innerWidth<=767)?0.52:0.8;`

**字号也要同步：** CSS媒体查询里的字号再缩20%（从当前值×0.8）
- rank: 9.6px → 7.7px
- suit: 8px → 6.4px
- center: 17.6px → 14px

---

## 改动2：电脑回放出牌区调大

**现状：** baseW=45（电脑端），用户说小了
**方案：** 改成 55

**改法：** JS `var baseW=(window.innerWidth<=767)?36:55;`

---

## 改动3：手机底牌缩小10%并上移

**现状：** 
- 底牌尺寸：41×54px（行434的CSS规则）
- 底牌位置：top:clamp(28px,8vh,46px)（横屏），top:3vh（竖屏）

**方案：**
- 尺寸缩小10%：41×54 → 37×49px
- 位置上移：横屏 top:clamp(8px,2vh,18px)，竖屏 top:1vh
- 只在手机端媒体查询里改，不影响电脑

**改法：** 在手机媒体查询CSS里加/改 `#landlord-area` 和 `#landlord-area .card` 规则

---

## 验证清单
- [ ] 手机回放：手牌、出牌区、底牌都缩小，花纹数字同步缩小
- [ ] 电脑回放：出牌区牌变大
- [ ] 手机正常游戏：底牌缩小并上移
- [ ] 电脑正常游戏：不受影响
- [ ] 控制台零报错

# 斗地主全站改造方案

## 概述

将项目 A（`C:\hermes 斗地主终极版`）的核心斗地主游戏整合到项目 B（`C:\hermes 斗地主终极版 软件`）的全站框架中，形成完整的"登录 → 大厅 → 游戏 → 战绩上报"闭环。

## 改造原则

1. **不动项目 A 的原始文件** — `C:\hermes 斗地主终极版\index.html` 保持原样
2. **只在项目 B 目录下操作** — `C:\hermes 斗地主终极版 软件`
3. **分步执行，每步可验证** — 每一步都有明确产出

---

## 第一步：创建 `game.html`（核心工作）

### 目标
把项目 A 的斗地主游戏整合到项目 B 中，作为 `frontend/game.html`。

### 需要修改的内容

**1. 从项目 A 复制的内容（保持原样）：**
- 全部 CSS 样式（`<style>` 标签内，约 590 行）
- 全部 HTML 结构（`<body>` 内，约 70 行）
- 全部 JavaScript 游戏逻辑（约 1600 行）

**2. 需要修改的地方：**

| 修改点 | 原因 | 具体改动 |
|--------|------|---------|
| 页面标题 | 从"斗地主终极版"改为"欢乐斗地主 - 游戏" | `<title>` 标签 |
| 音效系统 | 项目 A 没有音效文件，项目 B 有 40 个 mp3 | 引入 `sound.js`，替换原有音效调用 |
| 引入用户名 | 从 URL 参数获取登录用户名 | 添加 `?name=xxx` 参数解析 |
| 游戏结束回调 | 游戏结束后上报战绩给后端 API | 在结算时调用 `POST /api/game/end` |
| 返回大厅按钮 | 游戏结束后增加"返回大厅"按钮 | 在结算弹窗中增加按钮 |
| 音效文件路径 | 项目 B 音效在 `../audio/` | 修改音效加载路径 |
| 背景音乐 | 项目 A 使用 base64 编码的 BGM | 保留或移除 |

**3. 音效对接方案：**
- 项目 A 原有音效调用分散在游戏逻辑中（如 `new Audio('xxx.mp3')`）
- 方案：保持原有音效调用方式，但将音频文件路径指向 `../audio/` 目录
- 或者在 `game.html` 中引入 `sound.js` 并用其替换原有音效调用

### 产出物
`C:\hermes 斗地主终极版 软件\frontend\game.html`

---

## 第二步：打通游戏 → 后端战绩上报

### 目标
游戏结束后，将结果（胜负、角色、局数）上报给 Flask 后端。

### 需要在 game.html 中添加的代码

在游戏结算函数（`showResult` 或类似函数）中，增加：

```javascript
// 游戏结束时上报战绩
async function reportGameResult(result, role, rounds) {
    const name = new URLSearchParams(window.location.search).get('name');
    if (!name) return;
    try {
        await fetch('http://localhost:5000/api/game/end', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({
                name: name,
                result: result,  // 'win' 或 'lose'
                role: role,       // 'landlord' 或 'farmer'
                rounds: rounds,
                duration: 0
            })
        });
    } catch(e) {
        console.error('战绩上报失败:', e);
    }
}
```

### 验证方式
1. 本地启动 Flask 后端
2. 打开 `frontend/index.html` 登录
3. 进入大厅，点击开始游戏
4. 玩一局游戏，结束后检查数据库是否有记录

---

## 第三步：创建规则页面 `rules.html`

### 目标
一个简单的斗地主规则说明页面。

### 内容
- 斗地主基本规则
- 牌型说明（单张、对子、顺子、炸弹等）
- 叫地主规则
- 计分规则

### 入口
在大厅页面 `lobby.html` 增加"游戏规则"按钮

### 产出物
`C:\hermes 斗地主终极版 软件\frontend\rules.html`

---

## 第四步：创建设置页面 `settings.html`（可选）

### 目标
音效开关、退出登录、关于信息。

### 产出物
`C:\hermes 斗地主终极版 软件\frontend\settings.html`

---

## 第五步：本地测试验证

### 启动后端
```bash
cd C:\hermes 斗地主终极版 软件\backend
pip install -r ..\requirements.txt
python app.py
```

### 启动前端
直接用浏览器打开 `frontend/index.html`（或通过后端 `http://localhost:5000` 访问）

### 验证流程
1. 注册新用户 → 登录 → 进入大厅
2. 大厅显示战绩（0/0/0/0%）
3. 点击"开始游戏" → 进入游戏页
4. 完成一局游戏 → 自动返回大厅
5. 大厅战绩更新（1/1/0/100%）
6. 排行榜更新

---

## 第六步：部署上线

### 方案 A：Render 免费层（推荐）
- 项目 B 已有 `render.yaml` 和 `Procfile`
- 在 Render.com 创建 Web Service，关联 GitHub 仓库
- 自动部署，自动 HTTPS

### 方案 B：GitHub Pages + 单独后端
- 前端部署到 GitHub Pages
- 后端部署到 Render 免费层
- 需要修改前端 API 地址为 Render 的 URL

---

## 改造顺序与时间估计

| 步骤 | 内容 | 预估时间 |
|------|------|---------|
| 第一步 | 创建 `game.html`（核心） | 1 次执行 |
| 第二步 | 打通战绩上报 | 伴随第一步完成 |
| 第三步 | 创建规则页面 | 1 次执行 |
| 第四步 | 创建设置页面 | 可选 |
| 第五步 | 本地测试验证 | 1 次执行 |
| 第六步 | 部署上线 | 1 次执行 |

**总计：约 4-5 次执行可完成全部改造。**
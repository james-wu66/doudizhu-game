# 工作台资产状态说明（有效性核查）

> 由真实核实生成，记录工作台各资产当前有效性。
> 最近核查：2026-09-01。

## 背景

工作台于 8/31 建立，当时 AI 逻辑全部在前端 `frontend/index.html`。
9/1 项目重构为「后端 Python（ai_engine.py）+ 前端（game.js）」分离结构，AI 决策整体迁到后端。
因此早期部分资产仍指向**已不存在/已过时的旧前端位置**。

**2026-09-01 清理**：按用户要求，删除了工作台内部全部「已过时」资产
（目录结构.md / 测试报告 / 测试工具 / 测试用例），仅保留仍适用的资产。
规则：过时的东西，第二天即删除。

## 资产清单（当前有效）

| 资产 | 说明 |
|---|---|
| `提示词/TASK-001-手数分析.md` + `.docx` | 与后端 `estimate_hands` / `hand_bonus` 真实函数逐字吻合 |
| `提示词/TASK-002-拆牌罚分层级.md` + `.docx` | 与后端 `split_penalty` 真实函数吻合 |
| `提示词/TASK-005-概率记牌推算.md` + `.docx` | 与后端 `probably_has` / `estimate_*_in` 系列真实函数吻合 |
| `多AI三角色协作架构设计.md` | 协作架构说明，仍适用 |
| `团队/` 目录 | 三角色/多角色定义文档，协作参考 |
| `引擎/生成控制台.py` + `控制台模板.html` | 实时扫描真实项目、生成控制台 |
| `控制台.html` / `状态.json` / `协作状态.json` | 生成产物（真实扫描结果） |
| `启动工作台.bat` / `刷新工作台.bat` / `引擎/md2docx.py` | 运行与转换工具 |

## 已知真实状态要点

- **AI 成果在后端真实生效**：`backend/ai_engine.py` 含约 50 个函数，TASK-001/002/005 全部落地。
- **前端空壳已删除**：原 `game.js` 中 8 个「已迁后端」空壳函数（aiRouteValue / aiBombValue / aiControlTradeoff / aiCandidateScore / aiShouldPassCounter / aiPickScored / aiFindCounter / aiLead）已于 2026-09-01 删除。删除前已 grep 确认前端无任何调用点，删除安全；前端函数数 124→116。
- **后端 `ai_engine.py` 当前 0 个自动化测试守护**：项目根 `tests/` 仍覆盖 8/31 旧前端 `engine.js` 快照，未覆盖后端，且未提交 git。

## 刷新方式

双击 `刷新工作台.bat`，或终端执行：
```
cd "斗地主最终版工作台" && python 引擎/生成控制台.py
```

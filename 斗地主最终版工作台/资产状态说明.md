# 工作台资产状态说明（有效性核查）

> 本文件由真实核实生成，记录工作台中各资产的当前有效性。
> 核查时间：2026-09-01。核查方式：直接进入项目目录与文件逐项查看，非凭记忆。

## 背景

工作台于 8/31 建立，当时 AI 逻辑全部在前端 `frontend/index.html`。
9/1 项目重构为「后端 Python（ai_engine.py）+ 前端（game.js）」分离结构，AI 决策整体迁到后端。
因此部分早期资产仍指向**已不存在/已过时的旧前端位置**，需明确标注。

## 资产清单

### ✅ 有效（仍适用）

| 资产 | 原因 |
|---|---|
| `提示词/TASK-001-手数分析.md` | 与后端 `estimate_hands` / `hand_bonus` 真实函数逐字吻合 |
| `提示词/TASK-002-拆牌罚分层级.md` | 与后端 `split_penalty` 真实函数吻合 |
| `提示词/TASK-005-概率记牌推算.md` | 与后端 `probably_has` / `estimate_*_in` 系列真实函数吻合 |
| `多AI三角色协作架构设计.md` | 协作架构说明，仍适用 |
| `团队/` 目录 | 三角色/多角色定义文档，作协作参考仍有效（静态） |

### ⚠️ 已过时（未覆盖真实代码，勿据此判断项目状态）

| 资产 | 原因 |
|---|---|
| `目录结构.md` | 未同步最终结构；`README.md` 已是权威结构说明 |
| `测试报告/测试报告-TASK001-手数分析.md` | 基于未提交 git、且已与后端脱钩的旧前端 `tests/`；「41/41 通过」对当前后端无效 |
| `测试工具/GameSimulator.js` | 独立 JS 玩具，重写套牌逻辑，与真实后端引擎脱钩 |
| `测试工具/AIDecisionRecorder.js` | 独立脚本，未接真实引擎 |
| `测试工具/WinRateCalculator.js` | 独立脚本，未接真实引擎 |
| `测试用例/功能测试/` | 目录为空，占位测试从未调用真实函数 |
| 项目根 `tests/`（旧前端快照） | 覆盖 8/31 旧前端 `engine.js` 快照，未覆盖后端 `ai_engine.py`，且未提交 git |

## 当前真实状态要点

- **AI 成果在后端真实生效**：`backend/ai_engine.py` 含约 50 个函数，TASK-001/002/005 全部落地。
- **前端 `game.js`** 第 1477 行有注释：`已迁移到后端 API，保留空实现防引用报错`——那些 `return 0` 空壳是刻意的，不是成果丢失。
- **后端 `ai_engine.py` 当前 0 个自动化测试守护**（原前端 `tests/` 已失效）。
- **核心问题已修复**：原 `控制台.html` 是写死常量壳，现改为 `引擎/生成控制台.py` 实时扫描真实项目生成，控制台数据全部来自真实扫描。

## 刷新方式

双击 `刷新工作台.bat`，或终端执行：
```
cd "斗地主最终版工作台" && python 引擎/生成控制台.py
```

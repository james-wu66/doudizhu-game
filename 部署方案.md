# 斗地主全栈版部署方案 —— 腾讯云 CloudBase 云托管

## 给执行 AI 的话

本任务将 `C:\hermes 斗地主终极版 软件\` 部署到腾讯云 CloudBase 云托管（Cloud Run）。
用户已有腾讯云账号，环境 ID: `james-wu-d2gcojdjd404e6b8137`，免费体验版（至 2027-02-04）。

**铁律**：
- 改代码前先解释方案，用户确认后再动
- 每步做完自测
- 不破坏现有功能
- 前端版（C:\hermes 斗地主终极版）不碰

---

## 一、部署原理

```
用户手机/电脑
    ↓ 打开网址
腾讯云 CloudBase 云托管（Cloud Run 容器）
    ├── Flask 后端（app.py）← 处理 API 请求
    ├── 前端文件（index.html、lobby.html、sound.js）← Flask 直接 serve
    ├── 音频文件（audio/*.mp3）← Flask 直接 serve
    └── SQLite 数据库（database/doudizhu.db）← 容器内，重启会丢
```

**关键点：Flask 同时 serve 前端文件和 API，一个容器搞定全部。**

---

## 二、需要改的文件

### 1. 新建 Dockerfile（项目根目录）

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 复制全部文件
COPY backend/ backend/
COPY frontend/ frontend/
COPY audio/ audio/

# 云托管默认端口 8080
ENV PORT=8080

# 启动 Flask
CMD ["python", "backend/app.py"]
```

### 2. 新建 .dockerignore（项目根目录）

```
.git/
__pycache__/
*.pyc
.env
verify_replay.js
*.bat
*.md
```

### 3. 修改 backend/app.py（两处改动）

**改动1：端口从 5000 改为环境变量（云托管要求 8080）**

当前：
```python
app.run(host="0.0.0.0", port=5000, debug=True)
```

改为：
```python
import os
port = int(os.environ.get("PORT", 8080))
app.run(host="0.0.0.0", port=port, debug=False)
```

**改动2：静态文件路径改为绝对路径（容器内相对路径会变）**

当前 Flask 用 `send_from_directory("../frontend", ...)` 相对路径。
容器内工作目录是 /app，backend/app.py 在 /app/backend/ 下，
`../frontend` 实际指向 /app/frontend ✅ 路径是对的。

但需要确认 audio 路径也正确：`send_from_directory("../audio", ...)` → /app/audio ✅

**结论：路径不需要改，只需要改端口。**

### 4. 修改 requirements.txt

当前：
```
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
```

改为（加上 Pillow 用于头像压缩）：
```
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
Pillow>=10.0.0
```

---

## 三、部署步骤（手动部署，先跑通）

### 步骤1：本地准备部署文件

在项目根目录创建 Dockerfile 和 .dockerignore。

### 步骤2：安装 CloudBase CLI

```bash
npm install -g @cloudbase/cli
```

用户已有 tcb 命令（之前部署前端版用过）。

### 步骤3：登录

```bash
tcb login --apiKeyId <SecretId> --apiKey <SecretKey>
```

### 步骤4：创建云托管服务

在腾讯云控制台：
1. 进入 CloudBase 控制台
2. 选择环境 james-wu-d2gcojdjd404e6b8137
3. 左侧菜单 → 云托管
4. 新建服务 → 服务名: doudizhu
5. 选择"从本地目录部署"

### 步骤5：部署

```bash
cd C:\hermes 斗地主终极版 软件
tcb cloudrun deploy --envId james-wu-d2gcojdjd404e6b8137 --serviceName doudizhu
```

或者通过控制台上传 Dockerfile 所在目录。

### 步骤6：获取访问地址

部署成功后，云托会自动分配一个域名：
`https://doudizhu-xxx.james-wu-d2gcojdjd404e6b8137.service.tcloudbaseapp.com`

手机/电脑打开这个网址就能玩。

---

## 四、已知限制（先接受，后续优化）

| 问题 | 影响 | 后续方案 |
|------|------|---------|
| SQLite 重启丢数据 | 账号、战绩、回放重启后清零 | 换 MySQL |
| 头像重启丢失 | 上传的头像重启后没了 | 存腾讯云云存储 |
| 冷启动延迟 | 第一次访问等 5-10 秒 | 保持最少1个实例 |
| 免费额度有限 | 超出 3000 资源点要付费 | 监控用量 |

---

## 五、验证清单

- [ ] Dockerfile 创建成功
- [ ] .dockerignore 创建成功
- [ ] app.py 端口改为 8080
- [ ] requirements.txt 加了 Pillow
- [ ] tcb login 成功
- [ ] 云托管服务创建成功
- [ ] 部署成功，获取到访问地址
- [ ] 手机能打开网址
- [ ] 能注册/登录
- [ ] 能正常玩一局
- [ ] 音效正常
- [ ] 战绩/排行榜正常

---

## 六、后续优化（本次不做）

1. **换 MySQL** — 数据持久化
2. **接 GitHub 自动部署** — 推代码自动更新
3. **绑定自定义域名** — 更好记的网址
4. **前端单独部署到静态网站托管** — 加速访问

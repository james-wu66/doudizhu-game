# 斗地主项目完整记录（2026-08-12 最终版）

> **给接手人的说明：** 这是"会会斗地主"全栈项目的完整技术文档。所有信息已验证，可直接使用。

---

## 一、项目概况

| 项目 | 说明 |
|------|------|
| 项目名称 | 斗地主终极版（会会斗地主） |
| 技术栈 | Flask + SQLite/MySQL双模式 + HTML/CSS/JS 单文件 |
| 本地路径 | `C:\hermes 斗地主终极版 软件` |
| 前端版路径 | `C:\hermes 斗地主终极版`（纯前端备份，不要改） |
| GitHub | `james-wu66/doudizhu-game`（公开仓库） |
| 主分支 | `master`（本地）→ `main`（远程） |

---

## 二、腾讯云部署信息

| 项目 | 值 |
|------|-----|
| 主账号ID | `100047989601`（保密） |
| 云端地址 | https://doudizhu-game-294184-6-1420872702.sh.run.tcloudbase.com |
| 腾讯云环境ID | `james-wu-d2gcojd404e6b8137` |
| 资源点 | 40000点（已充VIP） |
| 部署方式 | push到main分支 → 腾讯云自动部署（不需要GitHub Actions） |
| 服务类型 | Cloud Run |
| 服务名 | `doudizhu-game` |
| 端口 | `8080`（app.py里配置） |

### 部署流程

```
本地 git push → GitHub main分支 → 腾讯云自动拉取代码 → 自动构建Docker → 自动部署
```

**注意：** 不需要手动部署，push到main就自动部署。

---

## 三、MySQL 数据库信息

| 项目 | 值 |
|------|-----|
| 类型 | CloudBase 自带 MySQL（CynosDB） |
| MySQL版本 | 8.0 |
| 集群ID | `cynosdbmysql-qt15dnr8` |
| 内网地址 | `172.17.0.2:3306` |
| 数据库名 | `james-wu-d2gcojd404e6b8137` |
| 用户名 | `doudizhu_game` |
| 密码 | `wzm13002104610.`（末尾有英文句号） |

### MySQL连接方式

```python
# 在 app.py 中已硬编码默认值（第15-19行）
DB_HOST = os.environ.get("DB_HOST", "172.17.0.2")
DB_PORT = int(os.environ.get("DB_PORT", "3306"))
DB_USER = os.environ.get("DB_USER", "doudizhu_game")
DB_PASSWORD = os.environ.get("DB_PASSWORD", "wzm13002104610.")
DB_NAME = os.environ.get("DB_NAME", "james-wu-d2gcojd404e6b8137")
```

**当前状态：** ✅ MySQL已正常工作（2026-08-12验证：注册、登录、战绩查询全部成功）

### 本地开发

本地开发时使用SQLite，不需要MySQL。`config_local.py` 用于本地MySQL测试（已在.gitignore中）。

---

## 四、GitHub Actions 问题

### 当前状态

GitHub Actions的 `tcb login` 步骤失败，报错：
```
Tencent Cloud Key verification failed
```

### 原因

GitHub Secrets里的 `TCB_SECRET_ID` 和 `TCB_SECRET_KEY` 可能：
- 没有配置
- 配错了值
- 被删除了

### 影响

**不影响实际部署。** 因为腾讯云有自己的自动部署机制（push到main触发），跟GitHub Actions是两条独立的路径。

### 如果想修复

1. 去 https://console.cloud.tencent.com/cam/capi 查看/生成API密钥
2. 去GitHub仓库 → Settings → Secrets and variables → Actions
3. 确保有以下Secrets：
   - `TCB_SECRET_ID` = 腾讯云SecretId
   - `TCB_SECRET_KEY` = 腾讯云SecretKey
   - `TCB_ENV_ID` = `james-wu-d2gcojd404e6b8137`
   - `DB_PASSWORD` = `wzm13002104610.`

---

## 五、已完成的功能（完整清单）

### 游戏核心
- ✅ 出牌、AI对手、牌型判断（15个AI决策函数）
- ✅ 叫地主/抢地主
- ✅ 记牌器、出牌提示、倒计时
- ✅ 春天检测、炸弹效果
- ✅ 出牌方向：逆时针

### 用户系统
- ✅ 登录/注册/游客模式
- ✅ 注销账号（需后端验证token）
- ✅ 头像上传
- ✅ 自动登录（token持久化）

### 音效系统
- ✅ 42个音效文件（40个语音 + 2个特效）
- ✅ YunjianNeural男声
- ✅ 背景音乐开关 + 音量滑块
- ✅ 音效默认开启

### 回放系统
- ✅ 出牌记录保存
- ✅ 回放模式（完整复现）
- ✅ 倍速控制
- ✅ 暂停/继续
- ✅ 前进/后退10秒

### 数据展示
- ✅ 战绩统计（胜率、连胜）
- ✅ 排行榜
- ✅ 个人信息中心
- ✅ 对局记录

### 手机适配
- ✅ 响应式布局
- ✅ 强制横屏
- ✅ 横屏阈值：<1100px
- ✅ Toast位置：画面正中
- ✅ 竖屏黑屏修复

---

## 六、2026-08-12 做的改动

### 1. 五项修复（已推GitHub，已部署）
- toast位置：横屏手机toast移到画面正中，不挡手牌
- 回放提示框：toast和bid-status缩小
- 顶牌花色：回放顶牌中央花色缩小10%
- 注销安全：注销后需后端验证token才能恢复登录
- 竖屏黑屏：transform加translateY(-100%)

### 2. MySQL双模式（已推GitHub，已部署）
- app.py改成双模式（SQLite/MySQL自动切换）
- MySQL配置硬编码默认值（不依赖环境变量）
- 本地开发用SQLite，云端用MySQL
- config_local.py存MySQL密钥（不推GitHub）

### 3. 手机版6项修复（已推GitHub，已部署）
- toast位置优化
- 竖屏黑边修复
- 弹窗滚动优化
- 滑块持久化
- 声音默认开启
- 大厅设置tab

---

## 七、文件结构

```
C:\hermes 斗地主终极版 软件\
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions配置（当前失败，不影响部署）
├── backend/
│   ├── app.py                  # Flask后端（24.5KB，双模式SQLite/MySQL）
│   ├── config_local.py         # MySQL密钥（不推GitHub，已在.gitignore）
│   ├── avatars/                # 用户头像目录
│   └── database/
│       └── doudizhu.db         # SQLite数据库（本地开发用）
├── frontend/
│   ├── index.html              # 主游戏页面（823KB，核心文件）
│   ├── lobby.html              # 大厅页面（37KB）
│   ├── manifest.json           # PWA配置
│   └── sound.js                # 音效系统（4.7KB）
├── audio/                      # 42个音效文件
│   ├── voice_*.mp3             # 40个语音
│   ├── effect_*.mp3            # 特效
│   └── 炸弹_大.mp3 / 补充_欢呼掌声.mp3
├── Dockerfile                  # 云端部署配置（Flask + gunicorn）
├── Procfile                    # 进程配置
├── render.yaml                 # Render部署配置（备用）
├── requirements.txt            # Python依赖（flask, flask-cors, gunicorn）
├── .gitignore                  # Git忽略规则
├── .dockerignore               # Docker忽略规则
├── 一键推送.bat                 # Git推送脚本
├── 一键部署.bat                 # 部署脚本
├── 启动斗地主.bat               # 本地启动脚本
├── verify_replay.js            # 回放验证脚本
├── 部署方案.md                  # 部署文档
├── 回放功能方案.md              # 回放功能设计文档
├── 手机端修复方案.md            # 手机端修复文档
├── 手机端回放缩放方案.md        # 手机端回放设计
├── 三问题精确修复方案.md        # 修复方案记录
└── 项目完整记录.md              # 本文件
```

---

## 八、关键技术细节

### 后端 app.py

- **框架：** Flask
- **数据库：** SQLite/MySQL双模式（自动切换）
- **端口：** 8080
- **API路由：**
  - `POST /api/register` - 注册（参数：name, password）
  - `POST /api/login` - 登录（参数：name, password）
  - `POST /api/auto-login` - 自动登录（参数：token）
  - `POST /api/logout` - 注销
  - `GET /api/stats/<name>` - 战绩统计
  - `GET /api/leaderboard` - 排行榜
  - `POST /api/save-game` - 保存对局记录
  - `GET /api/game-records/<name>` - 对局记录
  - `POST /api/upload-avatar` - 上传头像
  - `GET /api/avatar/<name>` - 获取头像

### 前端 index.html

- **大小：** 823KB（单文件，包含所有CSS/JS）
- **核心功能：** 游戏逻辑、UI、音效、回放
- **AI函数：** 15个（aiPlay, aiLead, aiDecideBid等）
- **关键全局变量：** 6个（必须在<script>开头声明）

### 数据库表结构

```sql
-- users表
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(64) NOT NULL,
    token VARCHAR(32),
    avatar VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- game_records表
CREATE TABLE game_records (
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    player_name VARCHAR(50),
    is_landlord BOOLEAN,
    result VARCHAR(10),  -- 'win' or 'lose'
    score INTEGER,
    played_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 九、已知问题

| 问题 | 状态 | 说明 |
|------|------|------|
| GitHub Actions失败 | ⚠️ 不影响 | Secrets里的API密钥问题，不影响实际部署 |
| 1111/1234旧数据丢失 | ❌ 原因不明 | 部署后旧数据丢失，新数据正常 |
| mimo模型不支持vision | 已知限制 | 图片识别需要用其他模型 |

---

## 十、本地开发

### 启动方式

```bash
# 方式1：双击 启动斗地主.bat
# 方式2：命令行
cd "C:\hermes 斗地主终极版 软件"
python backend/app.py
```

### 访问地址

- 本地：http://localhost:8080
- 云端：https://doudizhu-game-294184-6-1420872702.sh.run.tcloudbase.com

### 推送到GitHub

```bash
# 方式1：双击 一键推送.bat
# 方式2：命令行
cd "C:\hermes 斗地主终极版 软件"
git add .
git commit -m "描述你的改动"
git push origin master:main
```

---

## 十一、安全提醒

- ✅ 腾讯云SecretId/SecretKey不能泄露
- ✅ MySQL密码不能推到GitHub公开仓库
- ✅ config_local.py已加入.gitignore
- ✅ 密码已硬编码在app.py默认值里（安全但方便）
- ✅ 注销需后端验证token

---

## 十二、开发规范

1. **改代码前先检查：** 必须实际运行测试
2. **改完零问题：** 不是"没大问题"，是零问题
3. **密钥保密：** 不能推到GitHub公开仓库
4. **不改前端版：** `C:\hermes 斗地主终极版` 不动
5. **改前写方案：** 先解释方案，用户确认后再改
6. **每次改完推GitHub：** 自动部署到腾讯云
7. **手机端核心：** 用户手机横屏1024px，JS阈值<=767走不到，正确<1100

---

## 十三、联系方式

- GitHub: james-wu66
- 腾讯云主账号: 100047989601（保密）

---

*最后更新：2026-08-12*
